public class S1MSevenPuzzle {
    /*
    Breath first search
    expand, generate
    Dedup:
    hashMap<key = Board, value = Integer> which step this key was generated in
     */
    public int numOfSteps (int[] value) {
        return 0;
    }
}
